process {
    executor = 'local' 
    memory = '4 GB'    
    cpus = 2           
    time = '1h'       
    errorStrategy = 'retry'
}

workDir = './work'

log {
    enabled = true
    file = 'nextflow.log'
}
params {
    str = 'Hello World' 
}